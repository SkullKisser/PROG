#include <iostream>
using namespace std;

int main()
{
	float x1, x2, x3, y1, y2, y3, a, b, c, s, area;
	
	cout << "Type the coordinates of the first vertex: ";
	cin >> x1 >> y1;
	cout << "Type the coordinates of the second vertex: ";
	cin >> x2 >> y2;
	cout << "Type the coordinates of the third vertex: ";
	cin >> x3 >> y3;
	
	a = sqrt(pow(x1 - x2, 2) + pow(y1 - y2, 2));
	b = sqrt(pow(x2 - x3, 2) + pow(y2 - y3, 2));
	c = sqrt(pow(x3 - x1, 2) + pow(y3 - y1, 2));
	s = (a + b + c) / 2;
	area = sqrt(s*(s - a)*(s - b)*(s - c));

	cout << "The area of the triangle is: " << area;

	_getwch();
	return 0;
}
